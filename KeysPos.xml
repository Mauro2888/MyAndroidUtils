<meta-data
        android:name="com.api.name"
        android:value="@string/api_key"/>
build.gradle

android {

buildTypes {
    debug {
        resValue "string", "api_key", "[YOUR DEV KEY]"
    }
    release {
        resValue "string", "api_key", "[YOUR PROD KEY]"
    }
}}
